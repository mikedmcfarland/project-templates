{
  "name": "{{name}}",
  "version": "0.0.1",
  "description": "{{description}}",
  "main": "dist/{{name}}.js",
  "scripts": {
    "prepublish": "gulp build",
    "build": "gulp build",
    "test": "gulp test"
  },
  "author": "{{author}}",
  "devDependencies": {
    "engulped": "git://github.com/mikedmcfarland/engulped#master"
  }
}
